/* const createMens =  document.querySelector("[data-build = 'mensTableParent']"),
					table =	document.createElement("table"),
					thread = document.createElement("thead")
					tr = document.createElement("tr");
					th = [];

th.length = 8;

table.classList.add("table");

for(let i = 0; i < th.length; i++ ) {
	th[i] = document.createElement("th");
	 th[i].setAttribute("scope", "col");
	 th[i].innerHTML = "Hi";
	tr.append(th[i]);
}


createMens.append(table);
table.append(thread);
thread.append(tr); */